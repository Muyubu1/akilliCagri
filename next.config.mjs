/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/akilliCagri',
    assetPrefix: '/akilliCagri/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
